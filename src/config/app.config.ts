/**
 * Central app identity config.
 *
 * Renaming the app: change APP_NAME (and optionally APP_TAGLINE) here.
 * Also update `productName`/`name` in package.json and the <title> in
 * index.html to match — those three places are the only ones that
 * hardcode the app's name.
 */
export const APP_NAME = "Kana";
export const APP_TAGLINE = "Learn Japanese, one character at a time.";
export const APP_VERSION = "1.0.0";

/** localStorage key prefix — change if you rename the app and want a clean slate. */
export const STORAGE_PREFIX = "kana";
